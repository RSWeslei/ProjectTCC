import { StyleSheet } from "react-native";
import colors from "../../utils/globalColors";
import SignUpPrompt from "./index";

const SignUpPromptStyle = StyleSheet.create({
  mainContainer: {
    flex: 1,
    flexDirection: "column",
  },
})

export default SignUpPromptStyle;
